class Solution(object):
    def addBinary(self, a, b):

        ans = int(a,2) + int(b,2) #convert strings to binary ints, add them
        return(str(bin(ans))[2:]) 
        #return str of answer converted to binary from index 2 
        #onward to get rid of the binary '0b'
